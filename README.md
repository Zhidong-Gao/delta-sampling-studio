# Delta Sampling Studio

Web demo for **Delta Sampling** (NeurIPS 2026): training-free adapter transfer for diffusion models via noise-prediction delta injection.

This repo contains only the static frontend. Inference runs on a remote GPU backend reached via a tunnel — no compute happens on GitHub Pages.
